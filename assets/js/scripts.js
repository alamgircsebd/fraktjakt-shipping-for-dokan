( function ( $ ) {
	const FraktjaktShippingForDokanFrontendScripts = {
		init() {
			// add scripts
		},
	};

	$( function () {
		FraktjaktShippingForDokanFrontendScripts.init();
	} );
} )( jQuery );
